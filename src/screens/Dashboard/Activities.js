import React from 'react';
import {View, Text} from "react-native"

const Activities = () => {
    return (
        <View style={{flex: 1}}>
            
        </View>
    );
};

export default Activities;